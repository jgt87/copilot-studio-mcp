node_modules/
dist/
*.tsbuildinfo
.repowise/
.scratch/

# repowise editor wiring (machine-specific; regenerated by repowise init)
.mcp.json
.claude/CLAUDE.md

# Live verification: raw results carry tenant ids and message text.
# Share only the .redacted.md that scripts/redact-verification.mjs writes.
verification-results.md
*.results.md
!verification-results.redacted.md

# Scratch space for exports, unpacked solutions and verification runs.
temp/
tmp/
.temp/
.tmp/

# repowise: the decisions manifest is meant to be committed
!/.repowise/
/.repowise/*
!/.repowise/decisions.yaml

# The triage runbooks themselves are published. Their working folders are not: cases/ and
# samples/ hold clones of other people's agents, which must never reach this public repo.
runbooks/cases/*
!runbooks/cases/.gitkeep
runbooks/samples/*
!runbooks/samples/.gitkeep
